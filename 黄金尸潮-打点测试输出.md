# 黄金尸潮打点测试输出文档

## 一、测试准备
- 测试环境：未在输入材料中提供
- 测试方法（日志抓取/检索条件）：基于需求文档提取目标事件后，在 `log.txt` 中按目标账号和事件名检索；由于日志首行含说明文字且部分字符串字段含未转义双引号，分析时仅做只读清洗以恢复结构化解析
- 测试时间：2026-04-23 00:00:17 至 2026-04-23 10:41:12（以日志样本可见时间为准）
- 测试账号：未在材料中单独提供
- 目标 user_id：`1349349483`
- 日志时间范围：当前样本为“event.log 一小时内的日志”，实际内容覆盖 2026-04-23 00:00:17 至 2026-04-23 10:41:12
- 数据来源：
  - 需求文档（`.xlsx`）：`D:\Testwork\测试文档\打点测试文档\丧尸来袭\BM-黄金尸潮-打点&统计需求.xlsx`
  - 运行日志：`D:\Testwork\测试文档\打点测试文档\丧尸来袭\log.txt`
- 缺失信息说明：
  - 当前日志样本不覆盖完整活动周期，无法仅凭本次样本验证“本期玩家伤害排名结算”是否已实际触发
  - 事件体内未显式透传 `user_id` 字段，当前按日志顶层账号 key `1349349483` 进行归属判定

## 二、打点需求汇总
| 事件名 | 触发时机 | 参数 | 规则/口径 | 备注 |
|---|---|---|---|---|
| `escort_zombie_rally_appear` | 发现黄金尸王时上报 | `time`, `alliance_id`, `gid`, `lvl`, `expire_time`, `uuid` | 需求来源：`打点需求` Sheet 第 18-28 行 | 事件名定义在第 22 行 |
| `zombie_rally` | 攻击黄金尸王时上报 | `time`, `gid`, `lvl`, `alliance_id`, `type`, `win`, `damage`, `uuid`, `battle_id` | `type` 取值：发起集结=1，加入集结=0；`win` 取值：1=成功，0=失败 | 需求来源：第 30-43 行 |
| `zombie_user_settle` | 本期玩家伤害排名结算时上报 | `time`, `damage`, `rank` | 需求来源：第 45-52 行 | 仅能在结算场景验证 |

## 三、事件打点行为分析

### 3.1 事件：escort_zombie_rally_appear
- 测试场景：发现黄金尸王
- 查询条件：目标 `user_id=1349349483`；优先检索 `escort_zombie_rally_appear`，并对疑似同义事件 `zombie_rally_appear` 做补充核对
- 预期（来自需求）：事件名应为 `escort_zombie_rally_appear`，并携带 `time`, `alliance_id`, `gid`, `lvl`, `expire_time`, `uuid`
- 实际（来自日志）：未检出 `escort_zombie_rally_appear`；检出同义疑似事件 `zombie_rally_appear` 1 次，字段为 `time=882489600`, `alliance_id=435`, `gid=17039426`, `lvl=2`, `expire_time=1776912906244`, `uuid=2047142187602804700`
- 证据：
  - 日志行号：972
  - 时间戳：`1776911706253`，日期 `2026-04-23 10:35:06`
  - 关键片段：`"name": "zombie_rally_appear"`；其前置字段见 965-970 行
- 判定：不一致
- 问题类型：命名不一致
- 影响说明：如果数仓、埋点平台或统计口径按需求中的 `escort_zombie_rally_appear` 精确订阅，则该事件无法进入预期口径，导致“发现黄金尸王”链路漏数
- 修复建议：统一事件命名。若实现侧名称 `zombie_rally_appear` 为最终口径，则同步更新需求文档与下游配置；若需求为准，则代码改回 `escort_zombie_rally_appear`
- 截图：待补

### 3.2 事件：zombie_rally
- 测试场景：攻击黄金尸王
- 查询条件：目标 `user_id=1349349483`；事件名 `zombie_rally`
- 预期（来自需求）：事件名为 `zombie_rally`，并携带 `time`, `gid`, `lvl`, `alliance_id`, `type`, `win`, `damage`, `uuid`, `battle_id`；其中 `win` 应按 `1/0` 上报，`uuid` 需要可与“发现黄金尸王”日志关联
- 实际（来自日志）：共检出 5 次 `zombie_rally`，字段齐全，但存在两类偏差：
  - `uuid` 均为 `0`，未与发现事件中的 `uuid=2047142187602804700` 建立关联
  - `win` 实际上报为布尔值 `true/false`，而非需求中的 `1/0`
- 证据：
  - 日志行号：1119、1135、1151、2679、2695
  - 时间戳：`1776911857008`（3 次，日期 `2026-04-23 10:37:37`），`1776912072633`（2 次，日期 `2026-04-23 10:41:12`）
  - 关键片段：
    - `1114: "uuid": 0`
    - `1115: "win": false`
    - `1116: "damage": 2480283`
    - `2674: "uuid": 0`
    - `2675: "win": true`
    - `2676: "damage": 6109707`
- 判定：不一致
- 问题类型：取值异常 / 类型不一致
- 影响说明：
  - `uuid=0` 会导致攻击日志与发现日志无法按需求做链路关联，影响尸王生命周期分析和问题追踪
  - `win` 类型与需求不一致，可能导致 ETL、埋点校验器或报表枚举解析失败
- 修复建议：
  - 将 `uuid` 透传为对应尸王实例的真实唯一标识，并与发现事件保持一致
  - 明确 `win` 口径：若下游按整数枚举消费，则改为 `1/0`；若允许布尔值，则同步更新需求文档、校验规则和数仓映射
- 截图：待补

### 3.3 事件：zombie_user_settle
- 测试场景：本期玩家伤害排名结算
- 查询条件：目标 `user_id=1349349483`；事件名 `zombie_user_settle`
- 预期（来自需求）：结算时上报 `zombie_user_settle`，并携带 `time`, `damage`, `rank`
- 实际（来自日志）：当前样本未检出 `zombie_user_settle`
- 证据：
  - 日志行号：未检出
  - 时间戳：无
  - 关键片段：当前日志样本中无 `zombie_user_settle`
- 判定：待确认
- 问题类型：缺失
- 影响说明：当前无法确认结算埋点是否已实现，或仅因样本未覆盖结算时刻而未触发
- 修复建议：补充活动结算时段日志，或重放结算场景后再次核验
- 截图：待补

## 四、问题清单（汇总）
| 严重级别 | 事件名 | 参数 | 问题类型 | 问题描述 | 日志证据 | 需求证据 | 影响分析 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|---|---|---|
| S2 | `escort_zombie_rally_appear` | 事件名 | 命名不一致 | 需求要求 `escort_zombie_rally_appear`，日志实际为 `zombie_rally_appear` | `log.txt` 第 972 行 | `打点需求` Sheet 第 22 行 | 下游按需求事件名订阅时会漏统计发现尸王事件 | 统一实现、文档和下游口径的事件名 | 待修复 |
| S2 | `zombie_rally` | `uuid` | 取值异常 | 5 条攻击日志的 `uuid` 全为 `0`，无法与发现日志关联 | `log.txt` 第 1114、1130、1146、2674、2690 行 | `打点需求` Sheet 第 42 行 | 无法串联同一尸王实例，影响归因和链路分析 | 透传真实 `uuid` 并与发现事件保持一致 | 待修复 |
| S3 | `zombie_rally` | `win` | 类型不一致 | 需求约定 `1/0`，日志上报为 `true/false` | `log.txt` 第 1115、1131、1147、2675、2691 行 | `打点需求` Sheet 第 40 行 | 可能造成枚举解析或校验失败 | 统一 `win` 的上报类型或同步修正文档/映射 | 待确认 |
| S3 | `zombie_user_settle` | 事件级 | 缺失 | 当前样本未检出结算事件 | 当前样本无命中 | `打点需求` Sheet 第 49-52 行 | 结算链路暂不可验证 | 补充结算时段日志后复测 | 待确认 |

## 五、结论与建议
- 总体结论：当前日志样本可确认“攻击黄金尸王”事件已接入，但“发现黄金尸王”存在事件名偏差，“攻击黄金尸王”存在 `uuid` 和 `win` 口径问题；“本期玩家伤害排名结算”因样本时段不足暂无法定性
- 上线风险：中
- 账号匹配性说明：
  - 匹配目标 `user_id` 的日志数量：202（日志按顶层 key `1349349483` 归档）
  - 排除的非目标 `user_id` 日志数量：0
  - 缺失 `user_id` 字段的日志数量：202（事件体内未显式携带，当前依赖顶层 key 归属）
- 优先修复项：
1. 修正“发现黄金尸王”事件名与需求文档不一致的问题
2. 修正 `zombie_rally.uuid` 上报为 `0` 的问题，保证可与发现事件关联
3. 明确并统一 `zombie_rally.win` 的上报类型口径
- 回归建议：
  - 补抓完整活动链路日志：发现尸王、发起/参与集结、击杀成功/失败、结算发奖
  - 回归时重点验证同一尸王实例在 `zombie_rally_appear` 与 `zombie_rally` 中的 `uuid` 一致性
  - 在活动结算窗口单独抓取 `zombie_user_settle` 日志，补齐结算链路验证
