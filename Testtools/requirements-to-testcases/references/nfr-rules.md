# 非功能需求映射规则

常见 NFR 关键词与测试主题映射：

- 性能：延迟、吞吐、并发、资源占用
- 安全：认证、鉴权、注入、防敏感数据泄露
- 可靠性：重试、超时、熔断、恢复
- 可用性：降级、故障切换、健康检查
- 可观测性：日志完整性、指标、链路追踪

示例映射：

- “登录接口响应时间 < 200ms”
  -> 增加稳态与压测场景，并断言阈值。

- “只有管理员可以删除用户”
  -> 增加角色权限场景（管理员允许，非管理员拒绝）。

- “数据库短暂故障后系统可自动恢复”
  -> 增加故障注入 + 重试/恢复验证场景。